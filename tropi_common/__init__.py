"""tropi-service-common — shared plumbing for the Tropi Railway fleet.

Modules: excel_safe (SafeWorkbook), sentry (init_sentry), cc_track
(record_flow), telegram (send). Pin by commit SHA like the tropi adapters;
nothing here may contain business data (public repo).
"""
